请根据以下要求生成项目代码和 README。

## 项目主要需求

**项目概述**：电子病历管理系统（EMR）——为中小型医疗机构提供患者病历的数字化录入、存储、检索与共享，替代纸质病历流程，提升诊疗效率和数据安全性。
**项目类型**：全栈Web应用

### 功能需求

1. **患者档案管理**：支持录入患者基本信息（姓名、性别、年龄、身份证号、联系方式、过敏史、既往病史），支持通过姓名、身份证号或手机号快速检索患者档案，每个患者拥有唯一档案编号。

2. **病历书写与模板**：医生可为患者创建门诊/住院病历，支持结构化病历模板（主诉、现病史、既往史、体格检查、辅助检查、诊断、治疗方案），支持富文本编辑器记录病历内容，病历按时间轴展示。

3. **医嘱与处方管理**：医生可在病历中开具医嘱（长期医嘱、临时医嘱）和电子处方，处方包含药品名称、规格、用量、频次、疗程，支持处方预览和打印，处方状态跟踪（待审核、已审核、已执行）。

4. **病历权限与审签**：不同角色（医生、护士、管理员）拥有不同操作权限，医生书写的病历需上级医师审核签名后方可归档，已归档病历不可随意修改（需申请修改并留痕）。

5. **病历检索与统计**：支持按患者、科室、医生、时间段等多维度检索病历，支持病历关键词全文检索，提供科室病历工作量统计和疾病谱分析报表。

6. **系统管理与基础数据**：管理员可维护科室信息、医生账号、药品字典、诊断编码（ICD-10）、病历模板库，支持操作日志审计追踪。

### 技术要求

- **英文短名（用于 Docker Compose）**：`gbemr`
- **前端端口**：`FRONTEND_PORT=18930`
- **后端端口**：`BACKEND_PORT=19930`
- **前端框架**：React 18 + TypeScript + Ant Design
- **构建工具**：Vite
- **后端框架**：NestJS (Node.js)
- **数据库**：PostgreSQL
- **认证方式**：JWT + 角色权限控制
- **其他依赖**：富文本编辑器（如 @wangeditor/editor-for-react）

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：
页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbemr`
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：
- **不要写 `version:` 字段**（Compose V2 已废弃，会产生 `the attribute version is obsolete` 警告）
- 顶层声明 `name: gbemr` 作为兜底，避免目录名为中文时项目名为空
- 所有服务使用 `container_name` 命名容器，便于管理；容器名建议带 `${COMPOSE_PROJECT_NAME:-gbemr}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:<port>`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也建议配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `18930:80`，后端按框架默认端口 `19930:3000`
- 所有可变配置（端口、数据库账号密码、JWT 密钥等）通过环境变量注入
- 支持通过 `docker compose up -d` 一条命令启动全部服务，不依赖额外的 `-p` 参数

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbemr`
- 数据库连接相关：`DB_NAME` / `DB_USER` / `DB_PASSWORD`
- 后端：`JWT_SECRET`
- 端口映射：`FRONTEND_PORT=18930`、`BACKEND_PORT=19930`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:3000/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

自检清单（生成代码后必须确认）：
1. 在 **中文目录名** 下执行 `docker compose config --quiet` 不报错
2. 启动后 `docker compose ps` 显示全部容器 healthy
3. 浏览器访问前端端口能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`（不需要 `-p`）

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 首次启动前执行 `cp .env.example .env`

## docker-compose.yml 参考骨架（务必遵循）

```yaml
name: gbemr

services:
  db:
    image: postgres:15
    container_name: ${COMPOSE_PROJECT_NAME:-gbemr}-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    build: ./backend
    container_name: ${COMPOSE_PROJECT_NAME:-gbemr}-backend
    restart: unless-stopped
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      PORT: 3000
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-gbemr}-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-18930}:80"
    depends_on:
      - backend

volumes:
  db_data:
```

## Git 要求

代码编写完成后初始化 Git 并创建 `.gitignore`：

```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
```
